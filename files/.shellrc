#

myshell=bash
if [ -n "$ZSH_VERSION" ] ; then
  myshell=zsh
fi

LORDY_SOURCE ~/.zsh_aliases
LORDY_SOURCE ~/.bash_aliases

#export GCC_COLORS='error=01;31:warning=01;35:note=01;36:caret=01;32:locus=01:quote=01'
export WINEARCH=win32
export WINEPREFIX=/home/lordy/.wine32
# --- nvm -------------------------------------------------------------------
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"                  # loads nvm
[ -s "$NVM_DIR/${myshell}_completion" ] && \. "$NVM_DIR/${myshell}_completion"  # loads nvm completion (needs bashcompinit above)
export PATH="/home/lordy/.assemblyai-cli:$PATH"

# --- Google Cloud SDK ------------------------------------------------------
# zsh variants of the SDK include files (must come after compinit).
LORDY_SOURCE '/home/lordy/google-cloud-sdk/path.$myshell.inc'
LORDY_SOURCE '/home/lordy/google-cloud-sdk/completion.$myshell.inc'

export PYENV_ROOT="$HOME/.pyenv"
[[ -d $PYENV_ROOT/bin ]] && export PATH="$PYENV_ROOT/bin:$PATH"
eval "$(pyenv init - $myshell)"
eval "$(pyenv virtualenv-init -)"

LORDY_SOURCE ~/.fzf.$myshell

LORDY_ADD_PATH ~/bin
LORDY_ADD_PATH ~/.local/bin

export USE_BUILTIN_RIPGREP=0
export SSH_AUTH_SOCK="$(echo /tmp/ssh-*/agent.* )"

echo loaded $myshell
